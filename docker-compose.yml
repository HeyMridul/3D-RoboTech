services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: traic
      POSTGRES_PASSWORD: traic_dev
      POSTGRES_DB: traic
    ports:
      - "5432:5432"
    volumes:
      - traic_pg:/var/lib/postgresql/data

volumes:
  traic_pg:
