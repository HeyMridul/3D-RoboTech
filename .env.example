# PostgreSQL
DATABASE_URL="postgresql://traic:traic@localhost:5432/traic?schema=public"

# Auth.js (generate with: openssl rand -base64 32)
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Seed-only admin credentials
ADMIN_EMAIL="admin@traic.local"
ADMIN_PASSWORD=""

# Replaceable storage adapter (local is for development only)
STORAGE_PROVIDER="local"
STORAGE_LOCAL_PATH="./public/uploads"
